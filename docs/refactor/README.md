# Refactor Documentation Index

This directory groups the living documents that govern the DoctorDictate refactor effort:

- `refactoring-plan.md` – historical audit of the legacy structure with the target layout under `src/services/` and follow-on cleanup tasks.
- `../specs/performance-refactor-spec.md` – technical specification for the performance pipeline rewrite, now cross-referenced to ensure it honours the structural guidance from the plan.

Use this index as the landing spot when validating that implementation plans (or updates to the spec) stay aligned with the structural mandates captured in the plan.
